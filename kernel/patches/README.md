# Patch policy

No patches are currently carried. Add one only after recording the exact hardware
ID, failing log, upstream commit, affected kernel versions, test evidence, and
removal condition in `docs/compatibility.md`. Prefer a released upstream kernel
containing the fix.

